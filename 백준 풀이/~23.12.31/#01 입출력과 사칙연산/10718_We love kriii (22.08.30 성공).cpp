#include <iostream>

void init() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
}

int main() {
    init();

    std::cout << "강한친구 대한육군\n" << "강한친구 대한육군";

    return 0;
}